# mvt2json Insomnia plugin

A plugin to transform MVT tile HTTP responses to JSON responses.

The plugin catches all requests that have the following format : `places\/tile\/([a-z]+)\/(\d+)\/(\d+)\/(\d+)\.pbf`.

